<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Website</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/style.css">
    <style>
      body{
        background-image:url("./images/ggggg.jpg");
        background-size: cover;
      }
      .grid-container{
        display: grid;
        grid-template-columns: 2fr 1fr;
        gap: 2px;
      }
      @media (max-width: 800px){
        .grid-container{
          display: flex;
          flex-direction: column;
          flex : 1 ;
        }
      }
      h2{
        font-family: serif;
        text-shadow: 1px 1px 10px black;
      }
      svg{
        height: 25px;
        width: 30px;
        color: white;
      }
      .a{
        text-decoration: none;
      }
      .button{
        height: 100px;
        width: 250px;
        background-color: rgb(208, 223, 208);
        font-weight: bold;
        font-size: x-large;
        font-family: 'Times New Roman', Times, serif;
        color: black;
        border-radius: 10px;
        border: none;
        margin-top: 20px;
      }
      .button:hover{
        background-color: rgb(76, 154, 103);
        color: black ;
      }
      .division{
        display: flex;
        flex-direction: column-reverse;
        width: 30%;
        gap: 10px;
        margin-left: 50px;
      }
      .divisiont{
        display: flex;
        justify-content: center;
        align-items: center;
      }
      
    </style>
</head>
<body>
  <%- include("partials/header.ejs") %>
  <h2 style="margin: 30px 30px; color: white; text-align: center; font-size: 50px;">Blog Posts</h2>
      <div class="grid-container">
        <div class="division">
          <% user.forEach(userPost => { %>
            <a class="a" href="/blogs/<%= userPost.id %>">
            <div class="card">
              <div class="card-body">
                <h5 class="card-title"><%= userPost.title %></h5>
                <h6 class="card-subtitle mb-2 text-muted"><%= userPost.subject %></h6>
                <a href="/blogs/<%= userPost.id %>" class="card-link"></a>
              </div>
            </div>
            </a>
          <% }) %>
        </div>
        <div class="divisiont">
          <a href="./create"><button class="button">Create New Blog</button></a>
        </div>
      </div>
</body>
</html>